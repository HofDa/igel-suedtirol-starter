Follow the repository rules in `AGENTS.md`.
Never expose exact wildlife coordinates, never put the Supabase service-role key in client code, and never hardcode user-facing strings outside the translation files.
